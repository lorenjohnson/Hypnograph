- We need a player settings area which should be in its own new "Montage / Sequence Player" modal window which either takes over or replaces the HUD. The initial settings we need to integrate:
  - Max Number of Sources (for new random hypnograms) 
  - Length in time for the current playing/rendering Hypnogram
  - Frame Rate / Play Speed (I guess in FPS for now, but maybe better a % of normal speed)

- Lets's address the Frame Buffer refactor stuff here (moving the prefill out of Sequence view). We will employ the Rended playable "ready / prefilling" state stuff here and have an option for each player as to whether it starts rendering before the frame buffer is full or only after it is full and the effect is applied (which means possibly 2 seconds into playing). This player option should be called something like "Play With Effect" vs "Wait for Effect Buffer". This effect buffer wait shouldn't happen if the effect chain doesn't have any effects that use a frame buffer. 

- I would like when I save a Hypnogram that the JSON serialization of the Recipe also saves somewhere (named with a date/time stamp I guess). I would expect any current code we have for serializing is pretty out of date with the current recipe format so that needs throught through and caught-up. I would also like a Open option in the file menu for loading back a Hypnogram recipe (a .hypnogram file which will be json with comments)

- I currently have a Favorite feature. I would like when I save or favorite a Hypnogram fro it to go to a list that appears in another window that will display beneath the hud which lists all the favorited hypnograms. This will likely turn into a play list but for now i just want to be abel to click on them indivudally and have them load

- Respond to mic input or Midi Clock or OSC input from Live/etc?

- It would be nice to be able to type things and send them directly to the text overlay live! 

- I would like when I save a Hypnogram that the JSON serialization of the Recipe also saves somewhere (named with a date/time stamp I guess). I would expect any current code we have for serializing is pretty out of date with the current recipe format so that needs throught through and caught-up. I would also like a Open option in the file menu for loading back a Hypnogram recipe (a .hypnogram file which will be json with comments)

- Let's add the timeline editor for sources. Pretty much a basic timeline editor with the ablity to specify the Basic effects properties for each layer including Blend Mode.

- Performance Mode has a "Video Mixer" that specifies the transition between hypnograms. Right now it's a simple crossfade but we should make it configurable with 2-3 options. We would add these 2-4 options as push buttons in the header of the Preview panel in the main window.
