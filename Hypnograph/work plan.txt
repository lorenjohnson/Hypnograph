- Change the .hypnogram file extension to .hypno

- I would like to be able to click on the file/s used in the composition and open it either in Apple Photos or Finder (just opening a finder window to the file selected without actually opening it) if it is a file 

- Add Copy to Library button to effects items in editor?

- Add a Merge effects from Library button to Favorite items in the Hypnogram List?

document the effects library handling including the effeects library window
document the TextFieldFocusMonitor tab handling focus keybord handling in this app
document the render library 
document the favorites, save, and retrieval system as is

Bugs:

- Effect Manager bugs
  - Effect name editing is once AGAIN not working. It opens into edit mode but I can't type any text. the isTyping thing is again disconnected somehow. 
- Combine some/most/all of what is in the HUD into Player Settings modal
- Change the default locaiton of stored Hypnograms to ~/Documents/Hypnograph (or are there better alternatives?)
- I would like the window state to restore including whether clean screen is currently enabled
- When there were no windows in the saved window state then Tab toggles on all windows... may change this to just being a special keystroke for show all windows but not sure yet


- Add Loop true/false to Player Settings with true default and this means that sequence should also loop, and that Montage view shouldn't unless this is on. The attribute goes with the recipe. Make sure this same attribute is understood by all the players (i.e. by Performance Display and its preview). 


- Make favorites a playlist manager
  - Not sure I can Favorite a Sequence

- Move Divine into its own product, and focus it on ios usage possibly? It just looks at a single Apple Photos directory or file directory for simplicity

- Performance mode stress test: Test for perfomrance and crashing
- Fix still image support / test sequence mode integration with Live View... Ultiamtely can I compose montages and send them to the live display and will they fade between? Currently it seems to take forever for them to render when they are still images probably because of frame buffer contracts and such, but I they could/should be light at least when they are image only composions.

- Basic Sources Window ...
  - Let's add the timeline editor for sources. Pretty much a basic timeline editor with the ablity to specify the Basic effects properties for each layer including Blend Mode.
  - Finish scoping spacial transforms UI, layer editor, moving Diving into its own product.

---

- Respond to mic input or Midi Clock or OSC input from Live/etc?
- Update Game Controller to do only these things:
  - Next effect (E)
  - Previous Effect (Shift+E)
  - Save Hypnogram (the json) 
  - Save snapshot (S)
  - Select a favorite
  - Send to Performance Display (Return)
  - New Hypnogram (N)
  - Toggle clean screen on/off (the hammburger like button)

- Performance Mode has a "Video Mixer" that specifies the transition between hypnograms. Right now it's a simple crossfade but we should make it configurable with 2-3 options. We would add these 2-4 options as push buttons in the header of the Preview panel in the main window.
- Make a Quick Look player? / Hypnogram Player?
