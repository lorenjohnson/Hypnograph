services:
  hypnograph-site:
    image: nginx:1.27-alpine
    container_name: hypnograph-site
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
