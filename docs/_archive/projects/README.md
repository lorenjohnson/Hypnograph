# Archived Project Docs

Completed/superseded project docs live here to keep `docs/projects/` focused on active work.

Notes:
- This folder is excluded from default ripgrep/IDE searches via the repo `.ignore`.
- To search archived docs explicitly: `rg --no-ignore "pattern" docs/_archive`
