# Rules

Add project-local additions or overrides to canonical rules from product-context-manager `rules.md`.
Write one rule per line and use RFC-2119 terms (`MUST`, `SHOULD`, `MAY`, `MUST NOT`) when normative.

## Rules

- You SHOULD NOT do Xcode builds to check for compile errors, I will do that myself and report back
